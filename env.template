# ============================================
# VB-Lin_BackEnd - Variables de Entorno
# ============================================
# Copia este archivo a .env y ajusta los valores según tu entorno
# En Windows: copy env.template .env
# En Linux/Mac: cp env.template .env

# --------------------------------
# Servidor
# --------------------------------
PORT=4000
NODE_ENV=development

# --------------------------------
# JWT Tokens
# --------------------------------
# Secreto para access tokens (cambiar en producción)
JWT_SECRET=changeme_jwt_secret_production
# Tiempo de expiración de access token (formato: 2h, 30m, etc.)
JWT_ACCESS_EXPIRES=2h

# Secreto para refresh tokens (debe ser diferente al JWT_SECRET)
JWT_REFRESH_SECRET=changeme_refresh_secret_production
# Tiempo de expiración de refresh token (formato: 7d, 30d, etc.)
JWT_REFRESH_EXPIRES=7d

# --------------------------------
# MongoDB
# --------------------------------
# URI de conexión a MongoDB
# Para desarrollo local:
# MONGO_URI=mongodb://localhost:27017/VB-Lin_BackEnd
# Para Docker Compose (servicios):
# MONGO_URI=mongodb://mongo:27017/VB-Lin_BackEnd
# Para MongoDB Atlas:
# MONGO_URI=mongodb+srv://usuario:password@cluster.mongodb.net/VB-Lin_BackEnd
MONGO_URI=mongodb://localhost:27017/VB-Lin_BackEnd

# --------------------------------
# Redis
# --------------------------------
# Host de Redis
# Para desarrollo local: localhost
# Para Docker Compose: redis
REDIS_HOST=localhost
REDIS_PORT=6379

# --------------------------------
# SMTP (para envío de correos electrónicos)
# --------------------------------
# Host del servidor SMTP
SMTP_HOST=smtp.gmail.com
# Puerto del servidor SMTP (465 para SSL, 587 para TLS)
SMTP_PORT=465
# Usuario del servidor SMTP (tu email)
SMTP_USER=tu_email@gmail.com
# Contraseña del servidor SMTP o app password
SMTP_PASS=tu_app_password

# --------------------------------
# Firebase (para push notifications)
# --------------------------------
# Ruta al archivo de credenciales de Firebase Admin SDK (Service Account JSON)
# En Docker: /secrets/firebase.json
# En local: ./secrets/firebase.json (ruta relativa al proyecto)
# Si no se proporciona, las push notifications se simularán (solo se guardan en DB)
# IMPORTANTE: Necesitas obtener el Service Account JSON desde Firebase Console
# Ver: secrets/OBTENER_SERVICE_ACCOUNT.md para instrucciones detalladas
FIREBASE_CREDENTIALS_PATH=./secrets/firebase.json

# --------------------------------
# CORS
# --------------------------------
# Orígenes permitidos (separados por comas, sin espacios)
# Ejemplos:
# - Desarrollo local: http://localhost:3000,http://localhost:8080
# - Producción: https://tu-dominio.com,https://app.tu-dominio.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:9090

# --------------------------------
# Frontend URL (opcional)
# --------------------------------
# URL del frontend para enlaces en emails (reset password, etc.)
FRONTEND_URL=http://localhost:3000

